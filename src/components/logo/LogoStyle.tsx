import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  titulo: {
    textAlign: 'center',
    fontSize: 30,
    fontWeight: 'bold',
    color: 'yellow',
  },
  imagem: {
    resizeMode: 'contain',
    height: 100,
    width: 100,
  }
});